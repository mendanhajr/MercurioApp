import {StyleSheet} from "react-native";
const styles = StyleSheet.create({

    containerNota: {
        //width: '100%',
        elevation: 12,
        backgroundColor: '#fff',
        marginTop: 10,
        paddingTop: 20,
        paddingBottom: 20
    }
})

export default styles;